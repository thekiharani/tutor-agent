# Runtime only; scikit-learn and scipy are build-time (requirements-train.txt).
# Plain uvicorn, not uvicorn[standard]: the extras are 24MB of throughput
# tuning that means nothing here. Pinned so a rebuild gives the same links.
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5
numpy==2.5.3
nltk==3.10.3
