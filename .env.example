DEMO_PASSWORD=Demo@2026!
MOODLE_ADMIN_USER=admin
MOODLE_ADMIN_EMAIL=admin@example.com

# MOODLE_WWWROOT must match the address you type in the browser, port
# included, or Moodle redirects in a loop.
MOODLE_PORT=8080
MOODLE_WWWROOT=http://localhost:8080

# 1 shows PHP notices on the page; applies on restart.
MOODLE_DEBUG=0

# The demo courses are seeded on every container start. Safe to repeat: missing
# things are created, courses and activities are brought into line with
# seed_demo.php, and existing users and learning styles are left alone. Set to 0
# to leave the site empty.
SEED_ON_START=1

# 1 creates the demo teacher and the ten students. Production sets this to 0 and
# keeps only the administrator the installer makes from MOODLE_ADMIN_USER.
SEED_USERS=1

# The teacher on every course. Created whether or not SEED_USERS is on, because
# production wants a teacher too. Leave MOODLE_TEACHER_USER empty for none.
# MOODLE_TEACHER_PASSWORD falls back to DEMO_PASSWORD.
MOODLE_TEACHER_USER=demo.teacher
MOODLE_TEACHER_FIRSTNAME=Miriam
MOODLE_TEACHER_LASTNAME=Wafula
MOODLE_TEACHER_EMAIL=demo.teacher@example.com
MOODLE_TEACHER_PASSWORD=

RECOMMENDER_PORT=8000
# Below this confidence, fall back to a keyword lookup.
RECOMMENDER_THRESHOLD=0.45

POSTGRES_DB=moodle
POSTGRES_USER=moodle
POSTGRES_PASSWORD=moodle_demo_pw
