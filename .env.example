# `make up` copies this to .env if it is missing. Nothing here is secret: it is
# a throwaway local demo.

POSTGRES_DB=moodle
POSTGRES_USER=moodle
POSTGRES_PASSWORD=moodle_demo_pw

# MOODLE_WWWROOT must match the address you type in the browser, port included,
# or Moodle redirects in a loop.
MOODLE_PORT=8080
MOODLE_WWWROOT=http://localhost:8080
MOODLE_ADMIN_USER=admin
MOODLE_ADMIN_PASS=Admin#2026demo
MOODLE_ADMIN_EMAIL=admin@example.com
# 0 for the demo, 1 to show PHP notices while editing. Applies on restart.
MOODLE_DEBUG=0

RECOMMENDER_PORT=8000
# Below this confidence the service falls back to a keyword lookup.
RECOMMENDER_THRESHOLD=0.45

# Created by `make seed`.
DEMO_STUDENT_PASS=Student#2026demo
