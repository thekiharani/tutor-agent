# .env.example is committed; `make up` copies it to .env, which is per-machine.
.env
.env.local

# Written by train.py during the image build.
recommender/data/model.npz

# The project report is a working document, not a repo artefact.
*.docx

.DS_Store
.idea/
.vscode/
*.swp
__pycache__/
*.py[cod]
.venv/
